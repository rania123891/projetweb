<form style="width:100%" method="post" action="{{ path('app_typevehicule_delete', {'id': typevehicule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
   <input style="width:100%" type="hidden" name="_token" value="{{ csrf_token('delete' ~ typevehicule.id) }}">
     <button style="width:100%" class="btn btn-danger">Delete</button>
</form>
