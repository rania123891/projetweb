{% extends 'baseBack.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
<style>
  .pagination {
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 20px 0;
  }
  
  .pagination .current {
    font-weight: bold;
    padding: 5px 10px;
    background-color: #333;
    color: #fff;
    border-radius: 5px;
    margin-right: 10px;
  }
  
  .pagination .page a {
    display: inline-block;
    padding: 5px 10px;
    background-color: #eee;
    color: #333;
    border-radius: 5px;
    margin-right: 10px;
  }
  
  .pagination .page a:hover {
    background-color: #ccc;
  }
  
  .pagination .next a, .pagination .last a {
    display: inline-block;
    padding: 5px 10px;
    background-color: #eee;
    color: #333;
    border-radius: 5px;
    margin-right: 10px;
  }
  
  .pagination .next a:hover, .pagination .last a:hover {
    background-color: #ccc;
  }
  
</style>
  <body id="reportsPage">
    <div class="" id="home">
      <nav class="navbar navbar-expand-xl">
        <div class="container h-100">
          <a class="navbar-brand" href="index.html">
            {% if(is_granted('ROLE_ADMIN')) %}
            <h1 class="tm-site-title mb-0">Admin</h1>
            {% else %}
            <h1 class="tm-site-title mb-0">Mes Agences</h1>

            {%endif%}
          </a>
          <button
            class="navbar-toggler ml-auto mr-0"
            type="button"
            data-toggle="collapse"
            data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <i class="fas fa-bars tm-nav-icon"></i>
          </button>

          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto h-100">
              
              
              <li class="nav-item">
                <a class="nav-link active" href="/AfficheAgence">
                  <i class="fas  fa-user"></i> Agences
                </a>
              </li>

              <li class="nav-item">
                <a class="nav-link " href="/AfficheReservation">
                  <i class="fas fa-shopping-cart"></i> Reservations
                </a>
              </li>
           
            </ul>
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link d-block" href="login.html">
                  Admin, <b>Logout</b>
                </a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
        <div class="container" style="padding:0; margin:0;min-width:100%;"> 
            <div class="row" style="width:100%;padding:0; margin:0;">
                <div class="col" style="width:100%;padding:0; margin:0;">
<br>
                </div>
            </div>
            <!-- row -->
            <div class="row tm-content-row" style="width:100%">
                
                
              
                <div class="col-12 tm-block-col">
                    <div class="tm-bg-primary-dark tm-block tm-block-taller tm-block-scroll">
                        <h2 class="tm-block-title">Les Agences</h2>
                          <a class="btn btn-primary" href="/newagence" > Ajouter Agence</a>
                          <hr>
    <table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Propriétaire</th>
            <th scope="col">Nom</th>
            <th scope="col">Nombre</th>
            <th scope="col">Ref</th>
            <th scope="col">Email</th>
            <th scope="col">Adresse</th>
            <th scope="col">Ville</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        {% for a in pagination %}
            <tr>
                <td>{{ a.id }}</td>
                <td>{{ a.user.nom }}</td>
                <td>{{ a.NomAg }}</td>
                <td>{{ a.NombreAg }}</td>
                <td>{{ a.RefAg }}</td>
                <td>{{ a.EmailAg }}</td>
                <td>{{ a.AdresseAg }}</td>
                <td>{{ a.VilleAg }}</td>
                 <td>
            <a class="btn btn-primary" href="{{ path('deleteAgence', {'id': a.id}) }}"> supprimer </a>
            <a class="btn btn-primary" href="{{ path('edit', {'id': a.id}) }}"> modifier</a>
            <a class="btn btn-primary btn-pdf" href="{{ path('app_generate_agence_pdf', {'id': a.id}) }}">PDF</button>
        </td>
            </tr>
        {% endfor %}
    </tbody>
</table>

<div class="pagination" >
    {{ knp_pagination_render(pagination) }}
</div>

<script>
    $(document).ready(function() {
        $('button.btn-pdf').on('click', function() {
            var url = $(this).data('url');

            $.ajax({
                url: url,
                type: 'GET',
                dataType: 'binary',
                xhrFields: {
                    responseType: 'blob'
                },
                success: function(blob) {
                    var link = document.createElement('a');
                    link.href = window.URL.createObjectURL(blob);
                    link.download = 'agence.pdf';
                    document.body.appendChild(link);
                    link.click();
                    document.body.removeChild(link);
                },
                error: function(xhr, status, error) {
                    console.error(xhr.responseText);
                }
            });
        });
    });
</script>

                    </div>
                </div>
            </div>
        </div>
        <footer class="tm-footer row tm-mt-small">
            <div class="col-12 font-weight-light">
                <p class="text-center text-white mb-0 px-4 small">
                    Copyright &copy; <b>2018</b> All rights reserved. 
                    
                    Design: <a rel="nofollow noopener" href="https://templatemo.com" class="tm-footer-link">Template Mo</a>
                </p>
            </div>
        </footer>
    </div>

    <script src="../../Back/js/jquery-3.3.1.min.js"></script>
    <!-- https://jquery.com/download/ -->
    <script src="../../Back/js/moment.min.js"></script>
    <!-- https://momentjs.com/ -->
    <script src="../../Back/js/Chart.min.js"></script>
    <!-- http://www.chartjs.org/docs/latest/ -->
    <script src="../../Back/js/bootstrap.min.js"></script>
    <!-- https://getbootstrap.com/ -->
    <script src="../../Back/js/tooplate-scripts.js"></script>
    <script>
        Chart.defaults.global.defaultFontColor = 'white';
        let ctxLine,
            ctxBar,
            ctxPie,
            optionsLine,
            optionsBar,
            optionsPie,
            configLine,
            configBar,
            configPie,
            lineChart;
        barChart, pieChart;
        // DOM is ready
        $(function () {
            drawLineChart(); // Line Chart
            drawBarChart(); // Bar Chart
            drawPieChart(); // Pie Chart

            $(window).resize(function () {
                updateLineChart();
                updateBarChart();                
            });
        })
    </script>
</body>
{% endblock %}