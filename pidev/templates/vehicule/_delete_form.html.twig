<form method="post" action="{{ path('app_vehicule_delete', {'immatriculation': vehicule.immatriculation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicule.immatriculation) }}">
    <button class="btn btn-danger">Delete</button>
</form>
