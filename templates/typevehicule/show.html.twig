{% extends 'template.html.twig' %}

{% block title %}Typevehicule{% endblock %}
{% block nav %}
<li class="nav-item">
    <a href="" class="nav-link">
        <i class="nav-icon fas fa-copy"></i>
        <p>
            Paramètres
            <i class="fas fa-angle-left right"></i>
            <span class="badge badge-info right">2</span>
        </p>
    </a>
    <ul class="nav nav-treeview" style="display: block;">
        <li class="nav-item">
            <a href="{{ path ('app_vehicule_new') }}" class="nav-link ">
                <i class="far fa-circle nav-icon"></i>
                <p>Véhicules</p>
            </a>
        </li>
        <li class="nav-item">
            <a href="{{  path ('app_typevehicule_new')}}" class="nav-link active">
                <i class="far fa-circle nav-icon"></i>
                <p>Types Véhicules</p>
            </a>
        </li>


    </ul>
</li>
{% endblock nav %}
{% block body %}
    <h1>Typevehicule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typevehicule.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ typevehicule.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typevehicule.description }}</td>
            </tr>
        </tbody>
    </table>

    <button><a href="{{ path('app_typevehicule_index') }}">back to list</a></button>

    <button> <a href="{{ path('app_typevehicule_edit', {'id': typevehicule.id}) }}">edit</a></button>

    {{ include('typevehicule/_delete_form.html.twig') }}
{% endblock %}
