{% extends 'template.html.twig' %}

{% block title %}Typevehicule{% endblock %}

{% block body %}
    <h1>Typevehicule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typevehicule.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ typevehicule.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typevehicule.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_typevehicule_index') }}">back to list</a>

    <a href="{{ path('app_typevehicule_edit', {'id': typevehicule.id}) }}">edit</a>

    {{ include('typevehicule/_delete_form.html.twig') }}
{% endblock %}
