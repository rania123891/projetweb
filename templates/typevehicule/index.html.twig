{% extends 'template.html.twig' %}

{% block title %}Typevehicule index{% endblock %}

{% block body %}
    <h1>Typevehicule index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for typevehicule in typevehicules %}
            <tr>
                <td>{{ typevehicule.id }}</td>
                <td>{{ typevehicule.nom }}</td>
                <td>{{ typevehicule.description }}</td>
                <td>
                    <a href="{{ path('app_typevehicule_show', {'id': typevehicule.id}) }}">show</a>
                    <a href="{{ path('app_typevehicule_edit', {'id': typevehicule.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_typevehicule_new') }}">Create new</a>
{% endblock %}
